# VLA Inference Server Dependencies
# 서버에서 pip install -r requirements.txt 로 설치

# Core ML
torch>=2.0.0
torchvision>=0.15.0

# ZeroMQ 통신
pyzmq>=25.0.0
msgpack>=1.0.0

# 데이터 처리
numpy>=1.24.0
opencv-python>=4.8.0
pillow>=9.0.0

# LeRobot (SmolVLA, Pi0, GROOT 포함)
lerobot==0.4.3

# GROOT N1.5 추가 의존성 (groot 모델 사용 시 필요)
# 또는 lerobot_FMVLA 디렉토리에서: pip install -e ".[groot]"
peft>=0.13.0
dm-tree>=0.1.8
timm>=1.0.0
flash-attn>=2.5.9
ninja>=1.11.1
einops>=0.7.0

# 선택적: 디버깅
# ipython
# rich
